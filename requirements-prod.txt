# Production Dependencies for Render.com — MINIMAL BUILD
# Strategy: keep only what's strictly needed. Heavy/optional packages removed.
# - sentence-transformers (torch ~2GB) → removed
# - langchain (main) → removed (pulls langgraph/ormsgpack Rust build)
# - qdrant-client → removed (grpcio C extension, Qdrant vector search is optional)
# - spacy + presidio → removed (PII redaction is optional, spacy is heavy)
# - shap → removed (lazy import in prediction_explainer, optional)
# - biopython → removed (lazy import in hybrid_rag PubMed path, optional)
# - hiredis → removed (Redis C speedup, optional)
# - langchain-aws + boto3 → removed (Bedrock optional)

# Core
python-dotenv==1.0.0
pydantic-settings>=2.10.1
pydantic>=2.7.0

# Data Processing
pandas==2.2.0
numpy==1.26.3
scikit-learn==1.4.0
lightgbm==4.3.0

# Database
psycopg2-binary==2.9.9
sqlalchemy==2.0.25
alembic==1.13.1

# API Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
slowapi==0.1.9
httpx==0.27.0

# Security & Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Utilities
requests>=2.32.5
click==8.1.7
tenacity>=8.2.3

# Caching
redis==5.0.1

# Monitoring
prometheus-client==0.19.0
psutil==5.9.8

# LangChain 1.x — do NOT add langchain (pulls langgraph → ormsgpack Rust fail)
langchain-core>=1.0.0
langchain-community>=0.4.0
langchain-groq>=1.1.2
langchain-openai>=1.0.0

# RAG utilities (pure Python)
xmltodict>=0.13.0
tiktoken>=0.5.0
